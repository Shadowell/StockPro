## 页面单元 60：故障排查路径：页面、API、数据、策略

常见故障可按四层定位。页面层看按钮、空状态、筛选和浏览器请求；API 层看接口返回和错误 message；数据层看 K 线覆盖、同步任务和数据源；策略层看 on_bar 日志、broker 拒绝原因和持久化成交。

例如“策略不交易”可能是无数据、未预热、信号未通过、仓位达到上限、余额不足、合约 metadata 缺失、AI 模型失败或风控拒绝。逐层排查比直接修改策略参数更可靠。

> 本节对应截图：图13 - 模拟实例详情持仓成交。
