import { CheckCircle2 } from 'lucide-react'
export default function QualityPanel({items}:{items:Array<Record<string,any>>}){return <div><div className="mb-3 flex items-center gap-2"><CheckCircle2 className="h-4 w-4 text-blue-300"/><h2 className="text-sm font-semibold">质量问题</h2></div>{items.length?<div className="space-y-2">{items.map(item=><div key={String(item.id)} className="grid gap-2 rounded border border-crypto-border bg-crypto-bg/50 p-3 text-xs md:grid-cols-[120px_180px_1fr_160px]"><span className={item.severity==='blocking'?'text-red-300':item.severity==='warning'?'text-amber-200':'text-blue-200'}>{item.severity}</span><span className="font-mono text-gray-500">{item.dataset_code} / {item.partition_key}</span><span>{item.message}</span><span className="text-gray-600">{String(item.created_at||'').slice(0,19)}</span></div>)}</div>:<div className="rounded border border-dashed border-crypto-border p-8 text-center text-xs text-gray-500">没有质量问题记录</div>}</div>}
