import { useEffect,useState } from 'react'
import { Archive,CloudCog,Database,HardDriveDownload,RefreshCw,ServerCog,TableProperties } from 'lucide-react'
import type{LucideIcon}from'lucide-react'
import{dataCurrentApi}from'../api/client'
import{useAuth}from'../auth/AuthProvider'
import type{DataJob,DataStatus,DatasetRecord,ExtensionImport,SnapshotRecord}from'../types/data'
import ExtensionExchangePanel from'./data/ExtensionExchangePanel'
import QualityPanel from'./data/QualityPanel'
const tabs=['总览','研究数据','行情覆盖','同步任务','数据源','质量','导入导出']as const;type Tab=typeof tabs[number]
export default function DataManager(){const{role}=useAuth();const[tab,setTab]=useState<Tab>('总览');const[status,setStatus]=useState<DataStatus|null>(null);const[datasets,setDatasets]=useState<DatasetRecord[]>([]);const[snapshots,setSnapshots]=useState<SnapshotRecord[]>([]);const[jobs,setJobs]=useState<DataJob[]>([]);const[providers,setProviders]=useState<Array<Record<string,any>>>([]);const[quality,setQuality]=useState<Array<Record<string,any>>>([]);const[imports,setImports]=useState<ExtensionImport[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const load=async()=>{setLoading(true);try{const[s,d,ss,j,p,q,i]=await Promise.all([dataCurrentApi.status(),dataCurrentApi.datasets(),dataCurrentApi.snapshots(),dataCurrentApi.jobs(),dataCurrentApi.providers(),dataCurrentApi.quality(),dataCurrentApi.imports()]);setStatus(s);setDatasets(d.items);setSnapshots(ss.items);setJobs(j.items);setProviders(p.items);setQuality(q.items);setImports(i.items);setError('')}catch(e:any){setError(e?.response?.data?.detail||e?.message||'数据中心读取失败')}finally{setLoading(false)}};useEffect(()=>{void load()},[]);const metrics:Array<[string,string|number,LucideIcon]>=[['研究数据集',status?.datasets??'—',Database],['发布记录',status?.published_rows?.toLocaleString('zh-CN')??'—',TableProperties],['封存快照',status?.sealed_snapshots??'—',Archive],['Provider',status?.provider_state??'—',CloudCog]];return <div className="h-full overflow-y-auto bg-crypto-bg px-4 py-4 text-gray-100 sm:px-6"><header className="mb-4 flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-lg border border-blue-500/25 bg-blue-500/10"><Database className="h-5 w-5 text-blue-300"/></div><div><div className="text-[10px] font-semibold uppercase tracking-[.18em] text-blue-300/80">Data Trust</div><h1 className="text-xl font-bold">数据中心</h1></div></div><button type="button" onClick={()=>void load()} className="inline-flex items-center gap-1 rounded border border-crypto-border px-3 py-2 text-xs text-gray-400"><RefreshCw className={`h-3.5 w-3.5 ${loading?'animate-spin':''}`}/>刷新</button></header>{error&&<div className="mb-4 rounded border border-red-500/25 bg-red-500/10 p-3 text-xs text-red-200">{error}</div>}<div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([label,value,Icon])=><div key={label} className="rounded-xl border border-crypto-border bg-crypto-card p-4"><div className="flex justify-between text-[11px] text-gray-500"><span>{label}</span><Icon className="h-4 w-4 text-blue-400/70"/></div><div className="mt-2 truncate font-mono text-2xl font-semibold">{value}</div></div>)}</div><section className="mt-4 rounded-xl border border-crypto-border bg-crypto-card"><div role="tablist" className="flex overflow-x-auto border-b border-crypto-border p-2">{tabs.map(item=><button role="tab" aria-selected={tab===item} key={item} onClick={()=>setTab(item)} className={`shrink-0 rounded px-3 py-2 text-xs ${tab===item?'bg-blue-500/15 text-blue-200':'text-gray-500'}`}>{item}</button>)}</div><div className="p-4">{tab==='总览'&&<Overview status={status} snapshots={snapshots}/>} {tab==='研究数据'&&<Datasets items={datasets}/>} {tab==='行情覆盖'&&<Coverage items={datasets}/>} {tab==='同步任务'&&<Jobs items={jobs}/>} {tab==='数据源'&&<Providers items={providers}/>} {tab==='质量'&&<QualityPanel items={quality}/>} {tab==='导入导出'&&<ExtensionExchangePanel items={imports} admin={role==='admin'} onChanged={()=>void load()}/>}</div></section></div>}
function Overview({status,snapshots}:{status:DataStatus|null;snapshots:SnapshotRecord[]}){return <div className="grid gap-4 xl:grid-cols-2"><div className="rounded-lg border border-crypto-border bg-crypto-bg/50 p-4"><div className="flex items-center gap-2 text-sm font-semibold"><HardDriveDownload className="h-4 w-4 text-blue-300"/>封存研究快照</div><div className="mt-3 space-y-2">{snapshots.slice(0,8).map(item=><div key={item.id} className="flex justify-between rounded border border-crypto-border p-3 text-xs"><span>{item.name}</span><span className="text-emerald-300">{item.status} · {item.item_count}</span></div>)}</div></div><div className="rounded-lg border border-crypto-border bg-crypto-bg/50 p-4"><div className="flex items-center gap-2 text-sm font-semibold"><ServerCog className="h-4 w-4 text-blue-300"/>可信边界</div><div className="mt-3 space-y-2 text-xs text-gray-500">{[['存储',status?.storage],['Provider 状态',status?.provider_state],['GET Provider 调用',status?.provider_calls_performed],['质量问题',status?.quality_issues],['暂存导入',status?.staged_imports]].map(([label,value])=><div key={String(label)} className="flex justify-between border-b border-crypto-border py-2"><span>{label}</span><span className="font-mono text-gray-300">{value??'—'}</span></div>)}</div></div></div>}
function Datasets({items}:{items:DatasetRecord[]}){return <div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-xs"><thead className="text-[10px] text-gray-600"><tr>{['代码/名称','主源','整类回退','分区','记录','Watermark','状态'].map(x=><th key={x} className="border-b border-crypto-border px-3 py-2">{x}</th>)}</tr></thead><tbody>{items.map(x=><tr key={x.id} className="border-b border-crypto-border/60"><td className="px-3 py-2"><div>{x.name}</div><div className="font-mono text-[10px] text-gray-600">{x.code}</div></td><td className="px-3 py-2">{x.primary_source}</td><td className="px-3 py-2">{x.fallback_source||'—'}</td><td className="px-3 py-2 font-mono">{x.partition_count}</td><td className="px-3 py-2 font-mono">{Number(x.row_count).toLocaleString()}</td><td className="px-3 py-2">{String(x.last_published_trade_date||'—')}</td><td className="px-3 py-2">{x.enabled?'enabled':'disabled'}</td></tr>)}</tbody></table></div>}
function Coverage({items}:{items:DatasetRecord[]}){return <div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{items.map(x=><div key={x.id} className="rounded border border-crypto-border bg-crypto-bg/50 p-4"><div className="text-xs font-semibold">{x.name}</div><div className="mt-2 font-mono text-lg">{Number(x.row_count).toLocaleString()}</div><div className="mt-1 text-[10px] text-gray-600">{x.partition_count} partitions · {x.last_published_trade_date||'no watermark'}</div></div>)}</div>}
function Jobs({items}:{items:DataJob[]}){return <div className="space-y-2">{items.map(x=><div key={x.id} className="grid gap-2 rounded border border-crypto-border bg-crypto-bg/50 p-3 text-xs md:grid-cols-[1fr_100px_100px_180px]"><span>{x.job_name}</span><span>{x.source}</span><span>{x.status}</span><span className="text-gray-600">{String(x.created_at||'').slice(0,19)}</span></div>)}</div>}
function Providers({items}:{items:Array<Record<string,any>>}){return <div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-xs"><thead><tr>{['Dataset','Source','权限','缓存策略','导出策略','检查时间'].map(x=><th key={x} className="border-b border-crypto-border px-3 py-2 text-[10px] text-gray-600">{x}</th>)}</tr></thead><tbody>{items.map((x,i)=><tr key={i} className="border-b border-crypto-border/60"><td className="px-3 py-2">{x.dataset_code}</td><td className="px-3 py-2">{x.source}</td><td className="px-3 py-2">{x.permission_state}</td><td className="px-3 py-2">{x.cache_policy}</td><td className="px-3 py-2">{x.export_policy}</td><td className="px-3 py-2 text-gray-600">{String(x.checked_at||'').slice(0,19)}</td></tr>)}</tbody></table></div>}
