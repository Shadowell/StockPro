import{expect,test}from'@playwright/test';import{installFinalFixtures}from'./rebuild-final-fixtures'
test.skip(process.env.MOCK_API==='false','permission mutation fixtures are mock-only')
test('guest can read every owner page without mutation controls or requests',async({page})=>{await installFinalFixtures(page,'guest');const writes:string[]=[];page.on('request',request=>{if(['POST','PUT','PATCH','DELETE'].includes(request.method()))writes.push(request.url())});for(const route of['/','/market','/pools','/factors','/strategy','/backtest','/paper','/watch','/signals','/monitor','/review','/data','/ai-lab']){await page.goto(route);await expect(page.getByTestId('main-layout')).toBeVisible()}expect(writes).toEqual([]);await expect(page.getByRole('button',{name:'创建研究任务'})).toBeDisabled();for(const label of['创建模拟实例','暂存文件','评估告警','确认信号','封存'])await expect(page.getByRole('button',{name:label,exact:false})).toHaveCount(0)})
