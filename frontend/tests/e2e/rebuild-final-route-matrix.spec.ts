import{expect,test}from'@playwright/test'
import{installFinalFixtures}from'./rebuild-final-fixtures'
const routes:[string,string][]=[['/','A股市场总览'],['/market','A股行情'],['/pools','股票池'],['/factors','因子库'],['/strategy','策略中心'],['/backtest','回测控制台'],['/paper','模拟盘'],['/watch','盯盘'],['/signals','信号中心'],['/monitor','监控'],['/review','交易日复盘'],['/data','数据中心'],['/ai-lab','人工智能研发']]
for(const viewport of[{width:1440,height:900},{width:390,height:844}])test(`all owner routes satisfy operator shell at ${viewport.width}`,async({page})=>{test.setTimeout(360_000);if(process.env.MOCK_API!=='false')await installFinalFixtures(page);await page.setViewportSize(viewport);for(const[route,heading]of routes){await page.goto(route);await expect(page.getByTestId('main-layout')).toBeVisible({timeout:60_000});await expect(page.locator('[data-operator-page]')).toBeVisible({timeout:60_000});await expect(page.getByRole('heading',{name:heading}).first()).toBeVisible({timeout:60_000});if(process.env.MOCK_API==='false')await page.waitForLoadState('networkidle',{timeout:60_000});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1)).toBeTruthy()}})
