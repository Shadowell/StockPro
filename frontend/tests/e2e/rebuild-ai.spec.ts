import{expect,test}from'@playwright/test'
test('AI lab keeps BitPro workbench but cannot auto Paper',async({page})=>{await page.route('**/api/auth/me',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({auth_enabled:false,authenticated:true,role:'admin',permissions:['admin']})}));await page.route('**/api/ai/config',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({provider:'DashScope / Qwen',model:'qwen3.6-plus',model_state:'unavailable',execution_mode:'sealed_evidence_research',quick_backtest_only:true,auto_paper:false,mock_outputs:false})}));await page.route('**/api/ai/tasks*',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({items:[],total:0})}));await page.route('**/api/strategies',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({items:[]})}));await page.goto('/ai-lab');for(const tab of['自动研究','策略研发','现有策略优化'])await expect(page.getByRole('tab',{name:tab})).toBeVisible();await expect(page.getByText('模型未配置：任务启动会明确失败，不生成 mock、随机或模板结论。')).toBeVisible();await expect(page.getByText(/星球|Orbit|OKX持仓|自动实盘/)).toHaveCount(0);await expect(page.getByRole('button',{name:/启动.*Paper|自动模拟/})).toHaveCount(0)})
