import { expect, test } from '@playwright/test'
import { installFinalFixtures } from './rebuild-final-fixtures'
test('A-share spread and fundamental replacements keep honest source-backed states',async({page})=>{await installFinalFixtures(page);await page.goto('/arbitrage');await expect(page.getByText('A 股价差研究',{exact:true})).toBeVisible();await expect(page.getByText(/不生成虚假套利机会/)).toBeVisible();await page.goto('/onchain');await expect(page.getByText('A 股基本面与资金流',{exact:true})).toBeVisible();await expect(page.getByText(/尚未同步公告时点财务/)).toBeVisible();await expect(page.getByText('PE TTM',{exact:true})).toBeVisible();await expect(page.getByText(/DeFi|稳定币|协议研究|美元/)).toHaveCount(0)})
